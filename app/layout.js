export const metadata = {
  title: "Devis App",
  description: "Empty Next.js container for Orchids import"
};

export default function RootLayout({ children }) {
  return (
    <html lang="fr">
      <body>{children}</body>
    </html>
  );
}
